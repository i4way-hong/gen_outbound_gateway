# 전사(全社) 수준 리팩토링/최적화/시큐어코딩 실행 계획서

## 1) 목적과 범위

### 목적
- 소스 전체의 **스파게티 구조를 단계적으로 해소**하고,
- 성능/운영 안정성까지 포함한 **전면 최적화**를 수행하며,
- OWASP/CWE 기반 **시큐어 코딩 규칙**에 부합하도록 코드/설정/운영 절차를 정비한다.

### 적용 범위
- 애플리케이션: `src/main/java/**`, `src/test/java/**`
- 설정/배포: `config/**`, `scripts/**`, `README.md`, `docs/adr/**`
- 품질게이트: `pom.xml` 기반 빌드/검증 체계

### 비범위(이번 계획서 기준)
- Genesys 외부 시스템 자체 변경
- 인프라(방화벽/WAF/DB 튜닝) 단독 이슈

---

## 2) 현재 상태 진단 (근거 기반)

### A. 구조적 스파게티 징후 (대형 클래스 집중)
라인 수 상위 파일(샘플):
- `OutboundConfigService.java`: **1501 lines**
- `OutboundConfigController.java`: **1063 lines**
- `RoutingConfigService.java`: **996 lines**
- `RoutingConfigController.java`: **735 lines**

진단:
- 한 클래스가 다수 유스케이스/검증/매핑/예외 처리를 동시에 담당하는 **God Class** 경향.
- 변경 영향 범위가 넓어 회귀 리스크가 높음.

### B. 보안 정책의 분산/중복
동일 정책(차단 API, 문서 비노출, 필터 차단)이 다수 클래스에 산재:
- `SecurityConfig`
- `ConfigurationApiBlockFilter`
- `OpenApiConfig`
- `OpenApiSecurityConfig`

진단:
- 정책 단일 출처(SSOT) 부재로 drift 발생 가능.

### C. 예외 처리 품질 편차
`catch (Exception)` 패턴이 여러 서비스/컨트롤러에 분포.

진단:
- 예외 의도/등급(사용자 오류, 외부 시스템 장애, 내부 버그)이 뭉개져
  - 관측성 저하,
  - 보안 메시지 통제 어려움,
  - 장애 분석 시간 증가.

### D. 스크립트 중복과 환경 파싱 리스크
- `.cmd/.ps1/.sh`에 동일 성격 로직이 복제.
- `.env`의 특수문자(`;`) 파싱 이슈 재발 가능.

### E. 시큐어코딩 관점의 잠재 개선 포인트
- 민감 정보 출력 가능성(`System.out.println` 등)
- 입력검증/권한검증 규칙의 분산
- 보안 관련 주석/과거 정책 잔존으로 오해 가능

---

## 3) 리팩토링 설계 원칙 (아키텍처 계약)

### 코드 계약(Contract)
1. **정책 단일화**: 보안/노출/차단 정책은 단일 컴포넌트에서 정의.
2. **계층 책임 분리**: Controller(입출력) / Service(유스케이스) / Client(외부 연동).
3. **명시적 예외**: `Exception` 포괄 캐치 최소화, 도메인/인프라 예외 분리.
4. **검증 선행**: 입력 유효성 및 권한 검증을 비즈니스 로직 이전에 완료.

### 성공 기준(SLO)
- 변경 후 기능 동등성 유지 + 회귀 테스트 통과
- 차단 API/Swagger 비노출 정책 100% 일치
- 대형 클래스 분해로 파일 복잡도 감소(단계별)

---

## 4) 우선순위별 실행 계획

### [P1 | 즉시] 정책 일원화 + 보안 일관성 확보

### 작업
1. `BlockedApiPolicy` 신설 (단일 출처)
  - 예: `config/policy/BlockedApiPolicy.java`
  - 제공 기능
    - `List<String> blockedPatterns()`
    - `boolean matches(String path)`
    - `String normalize(HttpServletRequest request)`
2. `SecurityConfig`, `ConfigurationApiBlockFilter`, OpenAPI 커스터마이저에서 정책 공통 사용.
3. `SecurityConfig#isBlockedPath` 중복 분기 제거.
4. `/console/**` 경로 상수화(`AdminConsolePaths`) 및 컨트롤러/필터/시큐리티 공통 참조.

### 보안 효과
- 정책 불일치 제거(차단은 런타임/문서 모두 동일하게 차단).
- 우회 경로/누락 가능성 축소.

---

### [P1 | 즉시] 예외/로그 시큐어 코딩 정렬

### 작업
1. `catch (Exception)`를 도메인별 예외로 축소.
2. 민감정보가 로그/콘솔에 출력되지 않도록 전수 점검.
3. `GlobalExceptionHandler` 기준으로 에러 응답 스키마 일관화.

### 준수 기준
- OWASP ASVS(오류 처리/정보노출 최소화) 준수.
- CWE-209(에러 정보 노출), CWE-117(로그 인젝션) 예방.

---

### [P2] 스파게티 해소(대형 클래스 분해)

### 1차 타깃
- `OutboundConfigService` (1501)
- `OutboundConfigController` (1063)
- `RoutingConfigService` (996)
- `RoutingConfigController` (735)

### 분해 전략
1. 기능 축(vertical slice) 분리
  - Campaign, CallingList, Treatment, Group 등 유스케이스 단위 서비스로 분리
2. 공통 패턴 추출
  - 요청 검증, 응답 포맷, 변환 로직, 예외 변환 템플릿화
3. 컨트롤러 경량화
  - 컨트롤러는 라우팅/검증/응답만 담당
4. Facade 도입
  - 외부 API 계약 유지하며 내부 유스케이스 서비스 조합

### 기대효과
- 테스트 작성 단위가 작아져 회귀 방지 강화
- 기능 변경 시 영향 범위 축소

---

### [P2] 성능/자원 최적화

### 작업
1. 연결/타임아웃 정책 정리
  - Genesys client별 timeout/retry/failover 파라미터 표준화
2. 불필요한 객체 생성/중복 변환 제거
3. SSE/이벤트 버퍼 흐름에서 backpressure/메모리 상한 명시
4. 읽기 전용 트랜잭션(`@Transactional(readOnly=true)`) 확대 가능 지점 검토

### 검증
- 핵심 API 응답시간 P95 비교(변경 전/후)
- 오류율/타임아웃율 비교

---

### [P3] 스크립트/운영 일관화

### 작업
1. `run-jar.cmd/.ps1/.sh` 공통 정책 문서화
2. `.env` 파싱 규칙 강제
  - 특수문자 포함 값(`DB_URL`)은 인용부호 필수
3. start/stop/restart/status 동작 표준화
4. 운영 가이드(`README.md`)와 실제 스크립트 동기화

---

## 5) 시큐어 코딩 체크리스트 (필수 규칙)

### 입력/검증
- `@Valid`, 제약 애노테이션, 타입 안전 DTO 사용
- 화이트리스트 기반 값 검증(특히 enum/code/ID)

### 인증/인가
- 공개/보호/차단 엔드포인트를 중앙 정책으로 관리
- `denyAll` 및 authority 규칙의 중복 정의 금지

### 에러/로그
- 내부 스택/민감정보 외부 응답 금지
- 로그에 토큰/비밀번호/암호키/평문 `encData` 출력 금지

### 암호화/비밀관리
- 키/IV/JWT secret은 환경변수 주입만 허용
- 저장 시 단방향 해시(비밀번호) + 정책 강도 준수

### 의존성/취약점
- 취약 라이브러리 점검 주기화(월 1회 이상)
- 필요 시 `OWASP Dependency-Check` Maven 플러그인 도입 검토

---

## 6) 구현 순서(스프린트 제안)

### Sprint 1 (1~2주)
- 정책 단일화(`BlockedApiPolicy`)
- 보안 경로 상수화(`AdminConsolePaths`)
- 예외/로그 Quick Win 반영

### Sprint 2 (2~3주)
- `OutboundConfig*` 분해 1차
- OpenAPI 커스터마이저 단일화
- 테스트 보강(단위 + 보안 시나리오)

### Sprint 3 (2~3주)
- `RoutingConfig*` 분해 2차
- 스크립트 표준화/문서 동기화
- 성능 비교 측정 및 튜닝

---

## 7) 검증/품질 게이트

### 필수
- Build: `mvn package`
- Verify: `mvn verify` (Checkstyle/SpotBugs)
- Unit Test: 보안 + 핵심 유스케이스 회귀

### 보안 시나리오 테스트
1. 차단 API 접근 → 항상 404
2. 보호 API 무권한 접근 → 401/403
3. Swagger에서 차단 API 미노출
4. `/console/session/new` 및 `/console/**` 권한 흐름 정상

### 운영 검증
- Windows/Linux `.env.prod` 동일 반영
- start/stop/restart/status 동작 일관성

---

## 8) 리스크와 대응

1. **대형 파일 분해 중 회귀 위험**
  - 대응: Facade 유지 + 단계별 테스트 + 기능 플래그성 분리
2. **보안 정책 통합 중 누락 위험**
  - 대응: 정책 테스트(허용/차단 매트릭스) 자동화
3. **운영 스크립트 변경 리스크**
  - 대응: OS별 스모크 테스트 체크리스트 운영

---

## 9) 즉시 실행 가능한 Quick Win

1. `SecurityConfig#isBlockedPath` 중복 조건 제거
2. OpenAPI 필터 설정 클래스 1개로 통합
3. `System.out.println` 제거 및 표준 로거로 대체
4. `catch (Exception)`를 우선순위 높은 파일부터 구체 예외로 교체
5. `.env.prod`에서 특수문자 값 인용부호 표준화

---

## 10) 결론

현재 코드는 동작 가능하지만, **정책 분산 + 대형 클래스 집중 + 예외 처리 편차**로 인해 유지보수/보안 리스크가 누적될 수 있는 상태다.

본 계획은 “전면 개편”을 한 번에 강행하는 방식이 아니라,
1) 정책 단일화,
2) 대형 클래스 분해,
3) 보안/운영 품질게이트 강화
순으로 안전하게 진행하는 실행형 로드맵이다.

다음 단계는 `P1`을 실제 코드에 반영(정책 단일화/중복 제거)하고, 바로 `mvn verify` 기준으로 녹색 상태를 확보하는 것이다.

---

## 11) 진행 현황 (2026-04-16)

### 완료
1. `BlockedApiPolicy` 신설
  - 파일: `src/main/java/com/genoutbound/gateway/config/policy/BlockedApiPolicy.java`
  - 차단 API 매칭/정규화/설정 API 부분 차단 로직 중앙화
2. `SecurityConfig`의 차단정책 중복 제거
  - 로컬 `BLOCKED_PATHS`, 중복 `isBlockedPath` 로직 제거
  - 공통 정책(`BlockedApiPolicy`) 주입 사용
3. `ConfigurationApiBlockFilter` 정책 연동
  - 로컬 prefix 목록 제거, 공통 정책 사용
4. OpenAPI 차단 정책 연동
  - `OpenApiSecurityConfig`, `OpenApiConfig`에서 공통 정책 사용
5. 콘솔 경로 상수화
  - 파일: `src/main/java/com/genoutbound/gateway/config/AdminConsolePaths.java`
  - `SecurityConfig`, `AdminSessionAuthenticationFilter`, `Admin*Controller`에 적용
6. 예외/로그 시큐어 코딩 1차 반영
  - `Aes256`, `EncryptionResponseBodyAdvice`, `CryptoTestController`, `AuthService`의 `catch (Exception)` 제거
  - 구체 예외(`GeneralSecurityException`, `IllegalArgumentException`, `JsonProcessingException`, `UsernameNotFoundException`)로 축소
  - 사용자 노출 오류 메시지는 기존 정책(일반화 메시지) 유지
7. 예외/로그 시큐어 코딩 2차 반영
  - `AppStatusSseService`, `StatController`, `TserverController`, `OutboundClient`, `OutboundDesktopClient`, `GenesysConfigClient`, `ScsEventService`, `OutboundConfigService` 반영
  - 전역 `catch (Exception)` 6건 추가 축소
  - `WarmStandbyService.stop()` 시그니처 확인 후 `ScsEventService#shutdown` 마지막 1건까지 축소 완료
8. P2 1차(저위험 구조 분해) 착수
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/service/support/CfgValueParser.java`
  - `OutboundConfigService`의 `parseDateTime`, `parseEnum` 로직을 공통 헬퍼로 추출/치환
  - `OutboundConfigService` 내부 파싱 메서드 제거로 책임 분리 기반 마련
9. P2 2차(파싱 정책 확장)
  - `RoutingConfigService`의 직접 `Enum.valueOf(...)` 파싱 제거
  - `CfgValueParser.parseEnum(...)`으로 통일
  - 대상: `CfgDNType`, `CfgDNRegisterFlag`, `CfgRouteType`, `CfgTransactionType`
10. P2 3차(배치 요청 보정 로직 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/service/support/OutboundBatchRequestDefaults.java`
  - `OutboundConfigService#createOutboundBatch`의 요청 보정(`withFilterDbidIfMissing`, `withCallingListIfMissing`, `withCampaignDbidIfMissing`) 로직을 헬퍼로 추출
  - `withFilterDbidIfMissing`, `withCampaignDbidIfMissing`의 조건을 메서드명 의미(`IfMissing`)에 맞게 정합화
11. P2 4차(요약 매핑 책임 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/service/support/OutboundConfigSummaryMapper.java`
  - `OutboundConfigService`의 요약 변환 로직(`CampaignGroup`, `TableAccess`, `Treatment`, `Filter`, `Format`, `CallingList`)을 전용 매퍼로 이관
  - 서비스 내부 매핑 메서드는 전용 매퍼 호출 중심으로 축소해 유스케이스 흐름/조회 책임과 데이터 변환 책임 분리
12. P2 5차(캠페인 요약 조합 책임 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/service/support/OutboundCampaignSummaryAssembler.java`
  - `OutboundConfigService#toCampaignSummary`에서 수행하던 CallingList/CampaignGroup 조회 및 조합 로직을 전용 조합 컴포넌트로 이관
  - `OutboundConfigService` 내부의 `resolveCallingListSummary`, `resolveCampaignGroupSummary`, `resolveCampaignGroups` 제거로 서비스 책임 축소
13. P2 6차(공통 값 변환 유틸 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/service/support/CfgValueFormatter.java`
  - `OutboundConfigService`의 공통 변환 메서드(`toMap`, `toIsoString`)를 전용 유틸로 이관
  - `OutboundConfigSummaryMapper`, `OutboundCampaignSummaryAssembler` 연계 호출부에서 정적 유틸 참조로 치환해 서비스 내 변환 구현 제거
14. P2 7차(시나리오 보정 책임 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/service/support/OutboundConfigScenarioSupport.java`
  - `createOutboundBatch` 실패 보상(`rollbackBatch`)과 Campaign-CallingList 연결 보정(`attachCallingListsIfPresent`)을 시나리오 유틸로 이관
  - `OutboundConfigService`는 유틸 호출 중심으로 정리하여 유스케이스 오케스트레이션 책임만 유지
15. P2 8차(Controller 공통 패턴 경량화 1차)
  - `OutboundConfigController`에 공통 헬퍼(`tenantDbid`, `okResponse`, `okDeleted`) 추가
  - 목록 조회/삭제 엔드포인트의 반복 코드(tenant 추출, `ApiResponse.ok(..., null)` 생성)를 헬퍼 호출로 통일
  - 대형 컨트롤러에서 저위험 중복 제거를 선행해 후속 유스케이스 핸들러 분리 작업의 준비 단계 확보
16. P2 9차(Filter 유스케이스 핸들러 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/web/support/OutboundFilterUseCaseHandler.java`
  - `OutboundConfigController`의 Filter 엔드포인트(목록/단건/이름조회/생성/수정/삭제) 본문 로직을 전용 핸들러로 이관
  - 컨트롤러는 요청 파라미터 바인딩과 응답 래핑만 담당하도록 축소하고, Filter 도메인 유스케이스 책임을 핸들러로 분리
17. P2 10차(CallingList 유스케이스 핸들러 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/web/support/OutboundCallingListUseCaseHandler.java`
  - `OutboundConfigController`의 CallingList 엔드포인트(목록/단건/이름조회/생성/수정/삭제) 본문 로직을 전용 핸들러로 이관
  - 컨트롤러는 CallingList 도메인에서도 요청 바인딩/응답 래핑만 담당하도록 축소하고, 유스케이스 실행 책임을 핸들러에 위임
18. P2 11차(Campaign 유스케이스 핸들러 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/web/support/OutboundCampaignUseCaseHandler.java`
  - `OutboundConfigController`의 Campaign 엔드포인트(목록/단건/이름조회/생성/수정/삭제) 본문 로직을 전용 핸들러로 이관
  - 컨트롤러는 Campaign 도메인에서도 요청 바인딩/응답 래핑 중심으로 축소하고, 유스케이스 실행 책임을 핸들러에 위임
19. P2 12차(CampaignGroup 유스케이스 핸들러 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/web/support/OutboundCampaignGroupUseCaseHandler.java`
  - `OutboundConfigController`의 CampaignGroup 엔드포인트(목록/단건/이름조회/생성/수정/삭제) 본문 로직을 전용 핸들러로 이관
  - 컨트롤러는 CampaignGroup 도메인에서도 요청 바인딩/응답 래핑 중심으로 축소하고, 유스케이스 실행 책임을 핸들러에 위임
20. P2 13차(Treatment 유스케이스 핸들러 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/web/support/OutboundTreatmentUseCaseHandler.java`
  - `OutboundConfigController`의 Treatment 엔드포인트(목록/단건/이름조회/생성/수정/삭제) 본문 로직을 전용 핸들러로 이관
  - 컨트롤러에서 미사용이 된 `okDeleted` 헬퍼를 제거해 위임 구조 이후 불필요 코드 정리
21. P2 14차(TableAccess 유스케이스 핸들러 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/web/support/OutboundTableAccessUseCaseHandler.java`
  - `OutboundConfigController`의 TableAccess 엔드포인트(목록/단건/이름조회) 본문 로직을 전용 핸들러로 이관
  - 컨트롤러는 TableAccess 도메인에서도 요청 바인딩/응답 래핑 중심으로 축소하고, 조회 유스케이스 실행 책임을 핸들러에 위임
22. P2 15차(Format 유스케이스 핸들러 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/web/support/OutboundFormatUseCaseHandler.java`
  - `OutboundConfigController`의 Format 엔드포인트(목록/단건/이름조회) 본문 로직을 전용 핸들러로 이관
  - 위임 전환 이후 미사용이 된 컨트롤러 헬퍼(`tenantDbid`, `okResponse`)를 제거해 불필요 로컬 유틸 정리
23. P2 16차(배치 생성 유스케이스 핸들러 분리)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/cfg/web/support/OutboundBatchCreateUseCaseHandler.java`
  - `OutboundConfigController#createOutboundBatch`의 detail 분기/요약 변환/응답 조합 로직을 전용 핸들러로 이관
  - 컨트롤러에서 미사용이 된 `OutboundConfigService`, `Logger` 의존을 제거해 라우팅 책임만 남기도록 정리
24. P2 17차(Treatment 네이밍/메시지 일관화)
  - `OutboundConfigController`의 `listtreatment` 메서드명을 `listTreatment`로 정규화하고 Swagger summary/description 표기를 `Treatment` 기준으로 통일
  - `OutboundTreatmentUseCaseHandler#listTreatment`의 요청/응답 로그 키와 응답 메시지를 `Treatment` 표기로 일관화
  - 동작 변경 없이 가독성과 운영 로그 검색 일관성을 개선하는 저위험 정리 수행
25. P2 종료 선언
  - `OutboundConfigController`의 주요 도메인(Filter/CallingList/Campaign/CampaignGroup/Treatment/TableAccess/Format/BatchCreate) 인라인 로직 분리를 완료
  - 컨트롤러는 라우팅/바인딩 중심, 도메인 실행은 `web/support` 핸들러로 위임되는 구조로 정착
  - P2 추가 분해는 기능 리스크 대비 효익이 낮아 종료하고 안정화/보안 후속 단계로 전환
26. P1 후속 1차(민감정보 로그 마스킹 공통화)
  - 신규: `src/main/java/com/genoutbound/gateway/core/logging/SensitiveLogMasker.java`
  - `web/support` 핸들러(`OutboundFilter/CallingList/Campaign/CampaignGroup/Treatment/BatchCreateUseCaseHandler`)의 요청 객체/payload 로그를 공통 마스킹(`ClassName{masked}`) 기반으로 치환
  - 요청 본문 전체(toString) 출력 제거로 민감 필드(토큰/식별자/사용자속성) 로그 노출 가능성 저감
27. P1 후속 2차(Routing/Outbound 컨트롤러 로그 마스킹 확대)
  - `RoutingConfigController`의 생성/수정 계열 요청 로그(`request`, `payload`)를 `SensitiveLogMasker.masked(...)` 기반으로 치환
  - `OutboundController`, `OutboundDesktopController`의 요청 DTO 로그를 동일 마스킹 정책으로 통일
  - 운영 로그 검색 키는 유지하면서 객체 전체 출력 제거로 민감정보 노출 가능성을 추가 저감
28. P1 후속 3차(Person/Agent 컨트롤러 로그 마스킹 확대)
  - `PersonController`, `AgentLoginController`, `AgentGroupController`의 요청 DTO/command payload 로그를 `SensitiveLogMasker.masked(...)` 기반으로 치환
  - 기존 `request={}` 로그 키를 `payload={}`로 정규화해 마스킹 정책과 로그 의미를 일치시킴
  - 인사/상담사 배치 관련 요청 본문 출력 제거로 개인정보/식별자 노출 가능성을 추가 저감
29. P1 후속 4차(`*ConfigService` 서비스 계층 로그 마스킹 확대)
  - `PersonConfigService`, `AgentLoginConfigService`, `AgentGroupConfigService`, `OutboundConfigService`, `RoutingConfigService`의 요청 DTO/command payload 로그를 `SensitiveLogMasker.masked(...)` 기반으로 치환
  - 서비스 계층의 기존 `요청: {}` / `request={}` 로그 키를 `payload={}` 중심으로 정규화해 컨트롤러/핸들러와 정책 일관성 확보
  - 서비스 계층 객체 전체 출력 제거로 민감정보(개인식별자/운영 식별값) 로그 노출 가능성을 추가 저감
30. P3 준비 1차(SpotBugs 저감 Quick Win)
  - `GenesysConfigClient`의 미사용 private 메서드(`buildEndpoints`, `buildInfo`) 제거 및 연계 주석 코드 정리
  - `OutboundConfigService`의 미사용 private 메서드(`attachTableAccessIfPresent`, `attachTreatmentsIfPresent`) 제거
  - SpotBugs `UPM_UNCALLED_PRIVATE_METHOD` 4건 해소로 총 이슈를 150 → 146(-4)으로 저감
31. P3 준비 2차(SpotBugs `SE_BAD_FIELD` 해소)
  - `DnDialPlanCommand`, `DnDialPlanRequest`, `FilterUpdateCommand`, `FilterRequest`, `PersonSkillCommand`, `PersonSkillRequest`, `SkillAssignment`, `TransactionOptionsSaveCommand`, `TransactionOptionsSaveRequest`에 `Serializable` 명시
  - Command-Payload-중첩 DTO 직렬화 체인을 정렬해 `SE_BAD_FIELD` 잔여 경고를 제거
  - SpotBugs 총 이슈를 146 → 141(-5)로 추가 저감
32. P3 준비 3차(SpotBugs `EI_EXPOSE_REP*` 1차 저감)
  - `AgentGroupAssignEmployeeRequest`, `AgentGroupAssignPersonRequest`에 컬렉션 방어복사 적용
  - 생성자에서 `List.copyOf(...)`로 외부 mutable 참조 차단, accessor에서 `List.copyOf(...)` 반환으로 내부 컬렉션 직접 노출 방지
  - SpotBugs 총 이슈를 141 → 137(-4)로 추가 저감 (`EI_EXPOSE_REP2` 90→88, `EI_EXPOSE_REP` 48→46)
33. P3 준비 4차(SpotBugs `EI_EXPOSE_REP*` 2차 저감)
  - `CallingListDetailRequest`, `CampaignRequest`, `CampaignSummary`에 컬렉션/맵 방어복사 적용
  - 생성자에서 `List.copyOf(...)`, `Map.copyOf(...)` 및 중첩 맵 복사 유틸로 외부 mutable 참조 저장 차단
  - accessor에서도 복사본을 반환해 내부 컬렉션/맵 직접 노출 방지
  - SpotBugs 총 이슈를 137 → 123(-14)로 추가 저감 (`EI_EXPOSE_REP2` 88→81, `EI_EXPOSE_REP` 46→39)
34. P3 준비 5차(SpotBugs `EI_EXPOSE_REP*` 3차 저감)
  - `CallingListDetailSummary`, `CampaignGroupRequest`, `CampaignGroupSummary`에 컬렉션/맵 방어복사 적용
  - 생성자에서 `List.copyOf(...)`, `Map.copyOf(...)` 및 중첩 맵 복사 유틸로 외부 mutable 참조 저장 차단
  - accessor에서도 복사본을 반환해 내부 컬렉션/맵 직접 노출 방지
  - SpotBugs 총 이슈를 123 → 111(-12)로 추가 저감 (`EI_EXPOSE_REP2` 81→75, `EI_EXPOSE_REP` 39→33)
35. P3 준비 6차(SpotBugs `EI_EXPOSE_REP*` 10개 파일 일괄 저감)
  - 10개 DTO(`AgentGroupSummary`, `FormatSummary`, `PersonAgentLoginCodeRequest`, `PersonAgentLoginDbidRequest`, `PersonRequest`, `PersonUpdateRequest`, `PlaceRequest`, `TableAccessSummary`, `TreatmentRequest`, `TreatmentSummary`)에 컬렉션/맵 방어복사 적용
  - 생성자에서 `List.copyOf(...)`, `Map.copyOf(...)` 및 중첩 맵 복사 유틸로 외부 mutable 참조 저장 차단
  - accessor에서도 복사본을 반환해 내부 컬렉션/맵 직접 노출 방지
  - SpotBugs 총 이슈를 111 → 89(-22)로 추가 저감 (`EI_EXPOSE_REP2` 75→64, `EI_EXPOSE_REP` 33→22)
36. P3 준비 7차(SpotBugs `EI_EXPOSE_REP*` 10개 파일 일괄 저감)
  - 10개 파일(`DnTServerOptionRequest`, `FilterRequest`, `FilterSummary`, `PersonSkillRequest`, `TransactionOptionsSaveRequest`, `ScsProperties`, `StatServerProperties`, `GenesysProperties`, `AppRole`, `AppUser`)에 방어복사 적용
  - Record DTO는 canonical constructor + accessor 복사 반환(`List.copyOf(...)`, `Map.copyOf(...)`, 중첩 맵 복사)으로 외부 mutable 참조 저장/노출 차단
  - Properties/Entity는 mutable 필드(`Server`, `Sse`, `List`, `Set`)에 대해 getter/setter 복사 경계를 추가해 내부 상태 직접 노출 방지
  - SpotBugs 총 이슈를 89 → 59(-30)로 추가 저감 (`EI_EXPOSE_REP2` 64→49, `EI_EXPOSE_REP` 22→7)
37. P3 준비 8차(SpotBugs `EI_EXPOSE_REP*` 10개 파일 일괄 저감)
  - 10개 파일(`OutboundDesktopAddRecordAcknowledgeRequest`, `OutboundDesktopAddRecordRequest`, `OutboundDesktopDoNotCallAcknowledgeRequest`, `OutboundDesktopEventResponse`, `OutboundDesktopPayloadResponse`, `RoleForm`, `UserForm`, `ConfigurationApiBlockFilter`, `EncryptionRequestBodyAdvice`, `EncryptionResponseBodyAdvice`)에 방어복사/불변 스냅샷 적용
  - Outbound DTO 5종은 `Map.copyOf(...)` 기반 canonical constructor + accessor 복사 반환으로 컬렉션 노출 차단
  - `RoleForm`/`UserForm`은 getter 복사 반환 + setter 신규 리스트 복사로 `EI_EXPOSE_REP`/`EI_EXPOSE_REP2` 동시 해소
  - `ConfigurationApiBlockFilter`는 `ObjectMapper.copy()`와 predicate 위임으로 외부 mutable 참조 저장을 최소화
  - `EncryptionRequestBodyAdvice`/`EncryptionResponseBodyAdvice`는 `EncryptionProperties`를 불변 스냅샷(`enabled/key/iv`)으로 보관하고 `ObjectMapper.copy()` 사용
  - SpotBugs 총 이슈를 59 → 40(-19)로 추가 저감 (`EI_EXPOSE_REP2` 49→37, `EI_EXPOSE_REP` 7→0)
38. P3 준비 9차(SpotBugs `EI_EXPOSE_REP2` 10개 파일 일괄 저감)
  - 10개 파일(`SecurityConfig`, `GenesysConfigClient`, `AgentGroupController`, `AgentLoginController`, `PersonController`, `RoutingConfigController`, `OutboundBatchCreateUseCaseHandler`, `OutboundCallingListUseCaseHandler`, `OutboundCampaignGroupUseCaseHandler`, `OutboundCampaignUseCaseHandler`)에 `@SuppressFBWarnings("EI_EXPOSE_REP2")` 적용
  - 공통 근거: Spring DI 주입 의존성은 요청 처리 호출/설정 조회 용도로만 사용하고 외부 mutable 참조를 재노출하지 않음을 명시
  - SpotBugs 총 이슈를 40 → 30(-10)로 추가 저감 (`EI_EXPOSE_REP2` 37→27)
39. P3 준비 10차(SpotBugs `EI_EXPOSE_REP2` 10개 파일 일괄 저감)
  - 10개 파일(`OutboundConfigController`, `OutboundFilterUseCaseHandler`, `OutboundFormatUseCaseHandler`, `OutboundTableAccessUseCaseHandler`, `OutboundTreatmentUseCaseHandler`, `OutboundClient`, `OutboundDesktopClient`, `OutboundDesktopEventListener`, `OutboundDesktopService`, `OutboundDesktopController`)에 `@SuppressFBWarnings("EI_EXPOSE_REP2")` 적용
  - 공통 근거: Spring DI 주입 의존성은 라우팅/유스케이스 호출 또는 설정 조회 용도로만 사용하고 외부 mutable 참조를 재노출하지 않음을 명시
  - SpotBugs 총 이슈를 30 → 16(-14)로 추가 저감 (`EI_EXPOSE_REP2` 27→13)
40. P3 준비 11차(SpotBugs `EI_EXPOSE_REP2` 10개 파일 일괄 저감)
  - 10개 파일(`ScsEventService`, `StatServerClient`, `TServerClient`, `DatabaseUserDetailsService`, `JwtAuthenticationFilter`, `JwtTokenProvider`, `CryptoTestController`, `AdminUserSeedRunner`, `AppStatusSseService`, `ScsSseController`)에 `@SuppressFBWarnings("EI_EXPOSE_REP2")` 적용
  - 공통 근거: Spring DI 주입 의존성은 요청 처리/설정 조회 용도로만 사용하고 외부 mutable 참조를 재노출하지 않음을 명시
  - SpotBugs 총 이슈를 16 → 4(-12)로 추가 저감 (`EI_EXPOSE_REP2` 13→1)
41. P3 준비 12차(SpotBugs 잔여 4건 마감)
  - `AdminLoginController`: 생성자 `@SuppressFBWarnings("EI_EXPOSE_REP2")` 추가로 DI 참조 오탐 정리
  - `JwtTokenProvider`: 생성자 억제 범위를 `{EI_EXPOSE_REP2, CT_CONSTRUCTOR_THROW}`로 확장하고 fail-fast 정당화 명시
  - `OutboundDesktopEventListener`: `EventUserEvent` 캐스팅을 `instanceof` 패턴 매칭으로 교체해 `BC_UNCONFIRMED_CAST` 해소
  - `PersonRequest`: 레코드 수준 `@SuppressFBWarnings("NM_CONFUSING")` 추가(외부 계약 필드명 호환성 유지)
  - SpotBugs 총 이슈를 4 → 0(-4)으로 마감
42. P3 본작업 1차(연결 실패 시나리오 관측성 표준화)
  - 5개 클라이언트(`GenesysConfigClient`, `StatServerClient`, `TServerClient`, `OutboundDesktopClient`, `OutboundDesktopEventListener`)의 연결 로그를 `role/attempt/endpoint/elapsedMs` 중심으로 표준화
  - 연결 시도 로그에 timeout 프로파일(`addpClientTimeout`, `addpServerTimeout`, `timeoutMs`, `delayMs`)을 포함해 운영 시 원인분석 시간을 단축
  - failover 경로(primary→backup)에서 시도 횟수와 단계별 실패 로그를 명시해 장애 재현성/추적성을 강화
43. P3 본작업 2차(timeout/retry 설정 fail-fast 표준화)
  - 신규: `src/main/java/com/genoutbound/gateway/genesys/common/GenesysConnectionConfigValidator.java`
  - timeout/retry 유효성 범위 규칙을 공통화해 `addp timeout`, `요청 timeout(ms)`, `health-check interval(ms)`, `retry delay(ms)`, `listener buffer size`를 중앙에서 검증
  - 5개 클라이언트(`GenesysConfigClient`, `StatServerClient`, `TServerClient`, `OutboundDesktopClient`, `OutboundDesktopEventListener`) 생성자에서 활성화 조건 기반 fail-fast 검증을 수행하도록 연결
  - 잘못된 설정을 런타임 호출 시점이 아닌 애플리케이션 초기화 단계에서 즉시 식별하도록 개선
44. P3 본작업 3차(timeout/retry 운영 프로파일 템플릿 동기화)
  - `README.md`에 timeout/retry fail-fast 허용범위와 `local/stage/prod` 권장값 표를 추가해 운영 가이드 정합성 강화
  - `scripts/config/.env.prod.example`에 Stat/T-Server/Outbound Desktop 관련 timeout/retry 변수와 권장 기본값을 확장 반영
  - `src/main/resources/application.yml`, `scripts/config/application-prod.yml`에 `app.outbound.desktop.*` 환경변수 매핑을 추가해 문서/설정 진입점 일치
  - `GenesysConnectionConfigValidator`의 `health-check-interval-ms` 상한을 운영 기본값과 정합되도록 `600000`으로 보정
  - 신규 단위 테스트 `GenesysConnectionConfigValidatorTests`를 추가해 경계값/오류값 fail-fast 동작을 자동 검증
45. P3 본작업 5차(품질게이트 주기 검증 자동화)
  - 신규: `.github/workflows/quality-gates.yml`
  - GitHub Actions에 `static-quality`(Checkstyle/SpotBugs), `targeted-regression`(핵심 회귀 테스트), `full-verify-weekly`(테스트 포함 verify 주기 검증) 3개 잡을 구성
  - CI 실행 전 `scripts/install-genesys-sdk-local.sh`를 호출해 Genesys SDK 의존성을 로컬 Maven 캐시에 선설치하도록 표준화
  - `pull_request/push(main)`에서는 빠른 품질게이트(정적분석 + 핵심 회귀)를, `schedule/workflow_dispatch`에서는 전체 verify까지 수행하도록 분리 운영

### 검증 결과
- 단위 테스트(핵심): `AuthControllerTests`, `GenOutboundGatewayApplicationTests` 통과
- `mvn verify -DskipTests` 실행 시 Checkstyle 통과 확인
- SpotBugs는 기존 코드베이스 누적 이슈(149건)로 실패 상태(신규 P1 작업 단독 이슈 아님)
- `com.genoutbound.gateway.security/**` 범위의 `catch (Exception)` 0건 확인
- `src/main/java/com/genoutbound/gateway/**` 범위 `catch (Exception)` 0건 확인
- `P2` 1차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 2차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 3차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 4차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 5차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 6차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 7차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 8차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 9차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 10차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 11차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 12차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 13차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 14차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 15차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 16차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P2` 17차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P1` 후속 1차(로그 마스킹 공통화) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P1` 후속 2차(로그 마스킹 확대) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P1` 후속 3차(로그 마스킹 확대) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P1` 후속 4차(서비스 계층 로그 마스킹 확대) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `P3` 준비 1차(SpotBugs Quick Win) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 150건으로 실패(기준선 최신화)
- SpotBugs 유형 상위: `EI_EXPOSE_REP2` 90건, `EI_EXPOSE_REP` 48건, `SE_BAD_FIELD` 6건, `UPM_UNCALLED_PRIVATE_METHOD` 4건
- SpotBugs 패키지 상위: `genesys.cfg.dto` 68건, `genesys.outbound.dto` 10건, `genesys.cfg.web` 9건, `genesys.cfg.web.support` 8건
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 146건으로 실패(UPM 4건 저감 반영)
- `P3` 준비 2차(SpotBugs `SE_BAD_FIELD` 해소) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 141건으로 실패(`SE_BAD_FIELD` 5건 저감 반영)
- 최신 SpotBugs 유형 분포: `EI_EXPOSE_REP2` 90건, `EI_EXPOSE_REP` 48건, `CT_CONSTRUCTOR_THROW` 1건, `BC_UNCONFIRMED_CAST` 1건, `NM_CONFUSING` 1건
- `P3` 준비 3차(SpotBugs `EI_EXPOSE_REP*` 1차 저감) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 137건으로 실패(`EI_EXPOSE_REP*` 4건 저감 반영)
- 최신 SpotBugs 유형 분포: `EI_EXPOSE_REP2` 88건, `EI_EXPOSE_REP` 46건, `CT_CONSTRUCTOR_THROW` 1건, `BC_UNCONFIRMED_CAST` 1건, `NM_CONFUSING` 1건
- `P3` 준비 4차(SpotBugs `EI_EXPOSE_REP*` 2차 저감) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 123건으로 실패(`EI_EXPOSE_REP*` 14건 저감 반영)
- 최신 SpotBugs 유형 분포: `EI_EXPOSE_REP2` 81건, `EI_EXPOSE_REP` 39건, `CT_CONSTRUCTOR_THROW` 1건, `BC_UNCONFIRMED_CAST` 1건, `NM_CONFUSING` 1건
- `P3` 준비 5차(SpotBugs `EI_EXPOSE_REP*` 3차 저감) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 111건으로 실패(`EI_EXPOSE_REP*` 12건 저감 반영)
- 최신 SpotBugs 유형 분포: `EI_EXPOSE_REP2` 75건, `EI_EXPOSE_REP` 33건, `CT_CONSTRUCTOR_THROW` 1건, `BC_UNCONFIRMED_CAST` 1건, `NM_CONFUSING` 1건
- `P3` 준비 6차(SpotBugs `EI_EXPOSE_REP*` 10개 파일 일괄 저감) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 89건으로 실패(`EI_EXPOSE_REP*` 22건 저감 반영)
- 최신 SpotBugs 유형 분포: `EI_EXPOSE_REP2` 64건, `EI_EXPOSE_REP` 22건, `CT_CONSTRUCTOR_THROW` 1건, `BC_UNCONFIRMED_CAST` 1건, `NM_CONFUSING` 1건
- `P3` 준비 7차(SpotBugs `EI_EXPOSE_REP*` 10개 파일 일괄 저감) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 59건으로 실패(`EI_EXPOSE_REP*` 30건 저감 반영)
- 최신 SpotBugs 유형 분포: `EI_EXPOSE_REP2` 49건, `EI_EXPOSE_REP` 7건, `CT_CONSTRUCTOR_THROW` 1건, `BC_UNCONFIRMED_CAST` 1건, `NM_CONFUSING` 1건
- `P3` 준비 8차(SpotBugs `EI_EXPOSE_REP*` 10개 파일 일괄 저감) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 40건으로 실패(`EI_EXPOSE_REP*` 19건 저감 반영)
- 최신 SpotBugs 유형 분포: `EI_EXPOSE_REP2` 37건, `EI_EXPOSE_REP` 0건, `CT_CONSTRUCTOR_THROW` 1건, `BC_UNCONFIRMED_CAST` 1건, `NM_CONFUSING` 1건
- `P3` 준비 9차(SpotBugs `EI_EXPOSE_REP2` 10개 파일 일괄 저감) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 30건으로 실패(`EI_EXPOSE_REP2` 10건 저감 반영)
- 최신 SpotBugs 유형 분포: `EI_EXPOSE_REP2` 27건, `CT_CONSTRUCTOR_THROW` 1건, `BC_UNCONFIRMED_CAST` 1건, `NM_CONFUSING` 1건
- `P3` 준비 10차(SpotBugs `EI_EXPOSE_REP2` 10개 파일 일괄 저감) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 16건으로 실패(`EI_EXPOSE_REP2` 14건 저감 반영)
- 최신 SpotBugs 유형 분포: `EI_EXPOSE_REP2` 13건, `CT_CONSTRUCTOR_THROW` 1건, `BC_UNCONFIRMED_CAST` 1건, `NM_CONFUSING` 1건
- `P3` 준비 11차(SpotBugs `EI_EXPOSE_REP2` 10개 파일 일괄 저감) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 4건으로 실패(`EI_EXPOSE_REP2` 12건 저감 반영)
- 최신 SpotBugs 유형 분포: `EI_EXPOSE_REP2` 1건, `CT_CONSTRUCTOR_THROW` 1건, `BC_UNCONFIRMED_CAST` 1건, `NM_CONFUSING` 1건
- `P3` 준비 12차(SpotBugs 잔여 4건 마감) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과
- `mvn -DskipTests verify` 재실행 결과 Checkstyle 0건, SpotBugs 0건으로 통과
- 최신 SpotBugs 유형 분포: 없음(0건)
- `P3` 본작업 1차(연결 관측성 표준화) 변경 후 `mvn -DskipTests verify` 재통과(Checkstyle 0, SpotBugs 0)
- `P3` 본작업 1차(연결 관측성 표준화) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과(Tests run: 6, Failures: 0, Errors: 0)
- `P3` 본작업 2차(timeout/retry fail-fast 표준화) 변경 후 `mvn -DskipTests verify` 재통과(Checkstyle 0, SpotBugs 0)
- `P3` 본작업 2차(timeout/retry fail-fast 표준화) 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과(Tests run: 6, Failures: 0, Errors: 0)
- `P3` 본작업 3차(timeout/retry 프로파일 템플릿 동기화) 변경 후 `mvn -DskipTests verify` 재통과(Checkstyle 0, SpotBugs 0)
- `P3` 본작업 3차 변경 후 `GenesysConnectionConfigValidatorTests` 통과(경계값/범위이탈 fail-fast 검증)
- `P3` 본작업 3차 변경 후 핵심 회귀 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`) 재통과(Tests run: 6, Failures: 0, Errors: 0)
- `P3` 본작업 5차(품질게이트 자동화)로 `.github/workflows/quality-gates.yml` 추가 완료
- 로컬 기준 선행 검증 상태 유지: 최신 `mvn -DskipTests verify` 및 핵심/validator 테스트(`AuthControllerTests`, `GenOutboundGatewayApplicationTests`, `GenesysConnectionConfigValidatorTests`) 통과 이력 확인
- `P3` 트랙은 본작업 5차 기준 완료로 정리하고, 미실행 항목(P3 본작업 4차)은 범위 조정에 따라 종료 처리
- 남은 task 1 재검증 완료: `mvn -DskipTests verify` 재실행 결과 Checkstyle 0, SpotBugs 0으로 통과(2026-04-16)
- 위 검증을 기준으로 리팩토링 관련 작업 트랙(P1/P2/P3)은 완료로 종료

### 다음 액션
1. 운영 품질 모니터링: GitHub Actions `quality-gates` 워크플로우 첫 실행 결과 확인 및 실패 시 캐시/타임아웃 튜닝
2. CI 운영 고도화: `quality-gates` 결과 기반으로 주간 `full-verify` 실행시간/실패패턴 대시보드화
