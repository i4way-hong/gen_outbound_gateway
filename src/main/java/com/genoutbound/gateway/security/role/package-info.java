/**
 * 역할/권한 엔티티 패키지.
 *
 * <p>클래스 역할:
 * <ul>
 *   <li>{@code AppRole}: 사용자 역할 엔티티</li>
 *   <li>{@code AppRoleRepository}: 역할 저장소(JPA Repository)</li>
 *   <li>{@code AppPermission}: 세부 권한 엔티티</li>
 *   <li>{@code AppPermissionRepository}: 권한 저장소(JPA Repository)</li>
 * </ul>
 */
package com.genoutbound.gateway.security.role;
